{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the portfolio dashboard",
  "windows": ["main"],
  "permissions": ["core:default"]
}
