# ADR-004: Strict and Total LFL Scopes

**Status:** Accepted

The engine exposes Strict Comparable and Total Region scopes because stable-base performance and network-total performance answer different questions. Both use identical date alignment and formulas; only branch membership differs.
